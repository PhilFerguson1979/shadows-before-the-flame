// DM Tools Configuration
// Change this PIN to whatever you want. It's client-side only —
// keeps casual players from browsing DM content, but not hacker-proof.
export const DM_PIN = '6996';

// localStorage key used to store authentication state
export const DM_AUTH_KEY = 'sbtf-dm-authenticated';
